import React from 'react';

/**
 * Web stub for react-native's AppContainer (native-only).
 * Simply renders children for react-native-screens on web.
 */
export default function AppContainer({ children }) {
  return children;
}
